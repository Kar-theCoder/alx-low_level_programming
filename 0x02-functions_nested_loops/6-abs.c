#include "main.h"

/**
 * _abs - A function that computes absolute values of int
 *
 * @n: the number to be checked
 *
 * Return: absolute value of int
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
